# Dependencies
node_modules/

# Build output
dist/

# Vendored binaries (run "npm run prepare-vendor" locally after cloning)
vendor/

# Claude Code per-machine settings — see Team/Docs/ProcessRules.md "Claude Code Settings"
.claude/settings.local.json
.claude/.cache/

# Word documents (design notes, not source)
*.docx

# electron-builder debug artifacts
builder-debug.yml
builder-effective-config.yaml

# electron-builder cached binaries (downloaded on demand, not source)
scripts/rcedit-x64.exe
scripts/winCodeSign-*.7z

# One-off icon source (icon.ico is the shipping asset)
/comic.png

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.suo
*.user
